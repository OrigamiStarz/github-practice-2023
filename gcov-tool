const question = "What is 1+1?";
const correct = "2";
const userAnswer = prompt(question); 

const isCorrect = correct === userAnswer;

const result = isCorrect
  ? `good job, 1 + 1 is ${correct}.`
  : `your answer ${userAnswer} is wrong, how do you not know this?!`; 

console.log(result);
